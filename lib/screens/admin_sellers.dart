import 'package:flutter/material.dart';

class AdminSellers extends StatelessWidget {
  const AdminSellers({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Admin seller')),
      body: Center(
        child: Text('Admin seller'),
      ),
    );
  }
}
